[package]
name = "emusic-sid"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Safe Rust API over the vendored cRSID Commodore 64 SID engine"

[lib]
name = "emusic_sid"
path = "src/lib.rs"

[dependencies]
thiserror = "2"

[build-dependencies]
cc = "1"
