[package]
name = "emusic-milkdrop"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "CPU-side audio-reactive placeholder drawn when the projectM visualization is unavailable"

[lib]
name = "emusic_milkdrop"
path = "src/lib.rs"
